# 安装指南

您可以选择 Python 或 TypeScript 版本。两者提供完全相同的 `ddss` 命令行界面。

## Python 版本

### 使用 uvx（推荐）
```bash
uvx ddss
```

### 使用 pip
```bash
pip install ddss
ddss
```

## TypeScript 版本

### 使用 npx（推荐）
```bash
npx @hzhangxyz/ddss
```

### 使用 npm
```bash
npm install -g @hzhangxyz/ddss
ddss
```
